import { useState } from 'react';
import { render, screen } from '@testing-library/react'
import { OverlayProps, prefixCls } from './overlay'


describe('test control / un-control of Overlay component',()=>{
  it('should render the corrent default overlay', ()=> {

  })
})