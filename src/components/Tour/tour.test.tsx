import { useState } from 'react';
import { render, screen } from '@testing-library/react'
import { TourProps, prefixCls } from './tour'
import Tour from './tour'

describe('test control / un-control of Tour component',()=>{
  it('should render the corrent default Tour', ()=> {

  })
})